/**
 * ? ask for user name and
 * use @method prompt() to show the variable userName content in the console
 */

var userName = prompt("Su nombre? ", "Fulano de Tal");
console.log("Hola " + userName);
